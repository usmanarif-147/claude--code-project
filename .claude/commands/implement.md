---
description: Implement a new playground feature
---

Build the following feature for the admin playground section: $ARGUMENTS

Requirements:
- Create a Livewire component in app/Livewire/Admin/
- Create the blade view in resources/views/livewire/admin/
- Add a route in routes/web.php under the auth middleware group (playground section), with route name admin.playground.*
- Follow all conventions from CLAUDE.md
- Make it interactive and visually polished
- Include a page header matching the existing calculator page style (title + subtitle)
